const { circleArea, squareArea } = require('./shape-area');

console.log(circleArea(5));  // Example: Calling with a radius of 5
console.log(squareArea(4));  // Example: Calling with a side length of 4
